# Review Interface

Owns the local Barry Cache review UI, including the review server, review model, static UI assets, search groups, feature memory canvas, and implementation timeline canvas.

Use this pack for changes that affect `barry-cache review`, `/api/model`, review asset serving, review UI search, or the visual representation of memory and timeline records.
