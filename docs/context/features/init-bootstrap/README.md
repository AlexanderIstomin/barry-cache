# Init Bootstrap

Owns Barry Cache initialization behavior, including package script generation, agent adapter setup, and self-hosting rules for this repository.

Use this pack for changes that affect `barry-cache init`, generated agent instructions, package scripts, or preserving Barry memory inside the Barry Cache repository.
